# Hyper mappings:
# Yabai ------------------
# Desktop navigation UIOP
# Window navigation    JD
# Resize window        BN
# Next/Prev stack      FE
# Warp                  W
# Full screen           L
# Axis-y                H
# Stack                 T
# Balance               A
# Swap                  S
# Toggle sticky         Y
# Toggle float          Q
# Rotate 90 grades      X
# Restart               Z
# Recent window         _
# Kitty ------------------
# Next layout       SPACE
# Transparency         []
# Next/Prev tab        MK
# Create tab            G
# Rename tab            R
# Close tab             V
# Close window          C
# New window            :

# Desktops ------------
hyper - i : yabai -m space --focus 1 && yabai -m window --focus \$(yabai -m query --windows --space | jq .[0].id)
hyper - o : yabai -m space --focus 2 && yabai -m window --focus \$(yabai -m query --windows --space | jq .[0].id)
hyper - p : yabai -m space --focus 3 && yabai -m window --focus \$(yabai -m query --windows --space | jq .[0].id)
hyper - u : yabai -m space --focus 4 && yabai -m window --focus \$(yabai -m query --windows --space | jq .[0].id)

hyper - a : yabai -m space --balance

hyper - h : yabai -m space --mirror y-axis

hyper - x : yabai -m space --rotate 90

# Windows -------------
hyper - j : yabai -m window --focus east       || yabai -m window --focus west
hyper - d : yabai -m window --focus south      || yabai -m window --focus north
hyper - up : yabai -m window --focus recent    || yabai -m display --focus recent # managed by Karabiner
hyper - f : yabai -m window --focus stack.next || yabai -m window --focus stack.first
hyper - e : yabai -m window --focus stack.prev || yabai -m window --focus stack.last

hyper - s : yabai -m window --swap recent

hyper - w : yabai -m window --warp west || yabai -m window --warp east

hyper - t : yabai -m window --stack recent

hyper - n : yabai -m window --resize left:70:0    || yabai -m window --resize right:-70:0
hyper - b : yabai -m window --resize bottom:0:-70 || yabai -m window --resize top:0:70

hyper - q : yabai -m window --toggle float
hyper - y : yabai -m window --toggle sticky
hyper - l : yabai -m window --toggle zoom-fullscreen

hyper - 1 : yabai -m window --space 1
hyper - 2 : yabai -m window --space 2
hyper - 3 : yabai -m window --space 3
hyper - 4 : yabai -m window --space 4

# cmd - q : yabai -m window --close && yabai -m window --focus recent

# Misc ----------------
hyper - z : brew services restart yabai
