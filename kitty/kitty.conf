# startup_session ~/.config/kitty/startup.conf
# startup_session ./startup.conf
background_opacity .70
dynamic_background_opacity yes
# Be aware that using a value less than 1.0 in background_opacity is a (possibly significant) performance hit
# dynamic_background_opacity is off by default as it has a performance cost

background            black
# background            #272727
# background            #000030

# Font size (in pts)
font_size        18.0

font_family      Iosevka

confirm_os_window_close

macos_thicken_font 0.2
cursor_shape     block
cursor_blink_interval     0.5

cursor_stop_blinking_after 15.0

scrollback_lines 2000

# Program with which to view scrollback in a new window. The scrollback buffer is passed as
# STDIN to this program. If you change it, make sure the program you use can
# handle ANSI escape sequences for colors and text formatting.
scrollback_pager less +G -R

# Wheel scroll multiplier (modify the amount scrolled by the mouse wheel)
wheel_scroll_multiplier 5.0

# The interval between successive clicks to detect double/triple clicks (in seconds)
click_interval 0.5

# Characters considered part of a word when double clicking. In addition to these characters
# any character that is marked as an alpha-numeric character in the unicode
# database will be matched.
select_by_word_characters :@-./_~?&=%+#

# Hide mouse cursor after the specified number of seconds of the mouse not being used. Set to
# zero or a negative number to disable mouse cursor hiding.
mouse_hide_wait 0.0

# The enabled window layouts. A comma separated list of layout names. The special value * means
# all layouts. The first listed layout will be used as the startup layout.
# For a list of available layouts, see the file layouts.py
# enabled_layouts *
enabled_layouts stack, fat, tall

# If enabled, the window size will be remembered so that new instances of kitty will have the same
# size as the previous instance. If disabled, the window will initially have size configured
# by initial_window_width/height, in pixels.
remember_window_size   yes
#initial_window_width   640
#initial_window_height  400

# Delay (in milliseconds) between screen updates. Decreasing it, increases fps
# at the cost of more CPU usage. The default value yields ~100fps which is more
# that sufficient for most uses.
# repaint_delay    10
repaint_delay 0

# Delay (in milliseconds) before input from the program running in the terminal
# is processed. Note that decreasing it will increase responsiveness, but also
# increase CPU usage and might cause flicker in full screen programs that
# redraw the entire screen on each loop, because kitty is so fast that partial
# screen updates will be drawn.
# input_delay 3
input_delay 1

# Visual bell duration. Flash the screen when a bell occurs for the specified number of
# seconds. Set to zero to disable.
visual_bell_duration 0.0

# Enable/disable the audio bell. Useful in environments that require silence.
enable_audio_bell yes

# The modifier keys to press when clicking with the mouse on URLs to open the URL
open_url_modifiers ctrl+shift

# The program with which to open URLs that are clicked on. The special value "default" means to
# use the operating system's default URL handler.
open_url_with default

# The value of the TERM environment variable to set
term xterm-kitty

# The width (in pts) of window borders. Will be rounded to the nearest number of pixels based on screen resolution.
window_border_width 0

# Clipboard
map ctrl+shift+s        paste_from_selection
map super+c             copy_to_clipboard
map shift+insert        paste_from_selection

# Fontsize
map ctrl+shift+up      increase_font_size
map ctrl+shift+down    decrease_font_size
map ctrl+shift+backspace restore_font_size

map ctrl+g next_window
# map ctrl+t prev_window
map ctrl+t next_tab
map ctrl+shift+cmd+option+r new_window_with_cwd
# map ctrl+shift+cmd+option+l next_tab
map ctrl+shift+cmd+option+l last_used_layout
map ctrl+shift+cmd+option+space next_layout

map ctrl+shift+cmd+option+] set_background_opacity +0.1
map ctrl+shift+cmd+option+[ set_background_opacity -0.1

map ctrl+shift+cmd+option+v scroll_page_down
map ctrl+shift+cmd+option+c scroll_page_up

map ctrl+shift+t new_tab_with_cwd
map ctrl+shift+cmd+option+g new_tab_with_cwd

map ctrl+shift+cmd+option+a open_url_with_hints

window_border_width 1pt

macos_traditional_fullscreen no

# line up/down
# page up/down
# move window forward
# increase/decrease font size
# new tab (cmd + t)
# open url in web browser

# resize window
map ctrl+cmd+u resize_window taller 3
map ctrl+cmd+i resize_window shorter 3
map ctrl+cmd+o resize_window wider 3
map ctrl+cmd+y resize_window narrower 3

# tab bar
tab_bar_style hidden

cursor #cc231c
cursor_text_color #ebdbb2

inactive_text_alpha 0.65
hide_window_decorations yes
macos_option_as_alt no

macos_titlebar_color background

allow_remote_control yes

# GRUVBOX-DARK colorscheme for kitty
foreground            #ebdbb2
selection_foreground  #655b53
selection_background  #ebdbb2
url_color             #d65c0d

# black
color0   #272727
color8   #928373

# red
color1   #cc231c
color9   #fb4833

# green
color2   #989719
color10  #b8ba25

# yellow
color3   #d79920
color11  #fabc2e

# blue
color4  #448488
color12 #83a597

# magenta
color5   #b16185
color13  #d3859a

# cyan
color6   #689d69
color14  #8ec07b

# white
color7   #a89983
color15  #ebdbb2

active_tab_foreground  #655b53
active_tab_background  #ebdbb2
inactive_tab_foreground  #ebdbb2
inactive_tab_background  #655b53

# Karabiner kitty for reference (transparency)
# {
#   "conditions": [
#     {
#       "bundle_identifiers": ["^net\\.kovidgoyal\\.kitty$"],
#       "type": "frontmost_application_if"
#     }
#   ],
#   "from": {
#     "key_code": "open_bracket",
#     "modifiers": {
#       "mandatory": [
#         "left_shift",
#         "left_command",
#         "left_control",
#         "left_option"
#       ]
#     }
#   },
#   "to": [
#     {
#       "key_code": "a",
#       "modifiers": ["left_shift", "right_control"],
#       "hold_down_milliseconds": 100
#     },
#     {
#       "key_code": "l"
#     }
#   ],
#   "type": "basic"
# },
